{% extends 'admin/base.html.twig' %}

{% block title %}Admin commentaire
{% endblock %}

{% block body %}
<div class="container-fluid">
	<h1 class="my-5">
		Gestion des commentaire
	</h1>

	<table class="table table-hover">
		<thead>
			<tr>
				<th>Id</th>
				<th>Annonces</th>
				<th>Crée le</th>
				<th>Auteur</th>
				<th class="text-center">commentaire</th>
				<th class="text-center">Note</th>
				<th>Action</th>
			</tr>
		</thead>

		<tbody>
			{% for comment in pagination.data %}
				<tr>
					<td>{{comment.id}}</td>
					<td>
						{{comment.ad.title}}</td>
					<td>{{comment.createdAt |date("d/m/Y") }}</td>

					<td><img src="{{comment.author.picture}}" alt="avatar de {{comment.author.fullName}}" class="avatar avatar-micro mr-2">
						{{comment.author.fullName}}
					</td>
					<td class="text-center">{{comment.content }}</td>
				</tr>
			</span>
		</tbody>
	</td>
	<td>
		{{comment.rating}}
	</td>
	<td>
		<a href="{{ path('admin_comment_edit', {'id': comment.id}) }}" class="btn btn-primary">
			<i class="fas fa-edit"></i>
		</a>
		<a href="{{ path('admin_comment_delete', {'id': comment.id}) }}" class="btn btn-danger">
			<i class="fas fa-trash"></i>
		</a>
	</td>
</tr>{% endfor %}</table>{{ pagination.display() }}</div>{% endblock %}
